/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'eu.europa.ec.joinup.sd-dss.java-conventions'
}

dependencies {
    api project(':dss-asic-common')
    api project(':dss-cades')
    api project(':specs-asic-manifest')
    api project(':dss-service')
    api project(':dss-token')



    testImplementation files(project(':dss-test').sourceSets.test.output.classesDirs)
    testImplementation files(project(':dss-asic-common').sourceSets.test.output.classesDirs)
    testImplementation files(project(':specs-asic-manifest').sourceSets.test.output.classesDirs)


}

description = 'DSS ASiC with CAdES signature(s)'

tasks.register('testsJar', Jar) {
    archiveClassifier = 'tests'
    from(sourceSets.test.output)
}

publishing.publications.maven.artifact(testsJar)

java {
    withJavadocJar()
}

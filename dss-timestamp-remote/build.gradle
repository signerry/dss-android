/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'eu.europa.ec.joinup.sd-dss.java-conventions'
}

dependencies {
    api project(':dss-timestamp-dto')
    api project(':dss-spi')
    api project(':dss-xades')

    testImplementation files(project(':dss-xades').sourceSets.test.output.classesDirs)
    testImplementation files(project(':dss-test').sourceSets.test.output.classesDirs)
}

description = 'DSS Remote Timestamp Common'

tasks.register('testsJar', Jar) {
    archiveClassifier = 'tests'
    from(sourceSets.test.output)
}

publishing.publications.maven.artifact(testsJar)

java {
    withJavadocJar()
}

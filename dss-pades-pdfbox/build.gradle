/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'eu.europa.ec.joinup.sd-dss.java-conventions'
}

dependencies {
    implementation project(':dss-pades')
    implementation 'org.apache.pdfbox:pdfbox:2.0.25'
    testImplementation 'org.apache.pdfbox:preflight:2.0.25'
    testImplementation project(':dss-pades')
    testImplementation project(':dss-test')
    testImplementation 'org.junit.platform:junit-platform-suite-engine:1.8.2'
}

description = 'DSS PAdES using PDFBox'

tasks.register('testsJar', Jar) {
    archiveClassifier = 'tests'
    from(sourceSets.test.output)
}

publishing.publications.maven.artifact(testsJar)

java {
    withJavadocJar()
}
